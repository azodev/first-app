import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.azodev.firstApp',
  appName: 'First App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
